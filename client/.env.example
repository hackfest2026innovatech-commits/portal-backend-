# API base URL — set this to your deployed backend URL
VITE_API_URL=https://your-backend-url.com/api/v1

# Socket.io URL — set this to your deployed backend URL (no path)
VITE_SOCKET_URL=https://your-backend-url.com
